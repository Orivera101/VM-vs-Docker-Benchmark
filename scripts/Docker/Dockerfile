# Usa Nginx en versión liviana
FROM nginx:alpine

# Copia todo el contenido de EmulatorJS al HTML público
COPY ./emulatorjs/ /usr/share/nginx/html/

# Expone el puerto 80 para acceder por navegador
EXPOSE 80
